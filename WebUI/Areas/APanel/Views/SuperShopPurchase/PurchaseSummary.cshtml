@model IEnumerable<Library.ViewModel.Inventory.Purchases.SuperShopPurchaseViewModel>
@using ERP.WebUI.Html
@{
    ViewBag.Title = "PurchaseSummary";
}
@*<div class="row">
        <div class="col-lg-12">

            <h4 class="page-header">Purchase Information</h4>
        </div>
    </div>*@
@using (Html.BeginForm("PurchaseSummary", "SuperShopPurchase", FormMethod.Get, new { @id = "SearchForm" }))
{
    <div class="card card-inverse  ">
        <div class="card-header alert-warning h5">
            Purchase Summary
            <a href="/APanel" class="btn btn-bg pull-right btn-sm" role="button">Back</a>
        </div>
        <div class="card-body">
            <div class="col-12">
                <div class="row">
                    <div class="col-2">
                        <div class="form-group">
                            <label class="control-label   mendatory">Company</label>
                            @Html.SimpleDropDown("companyId", "/APanel/Company/GetCompanyList/", new { @class = "form-control", data_val = "true", data_val_required = "This is required" })
                            @Html.ValidationMessage("companyId", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="col-2">
                        <div class="form-group">
                            <label class="control-label  ">Branch</label>
                            @Html.CascadingDropDown("branchId", "/APanel/Branch/GetBranchList/", "companyId", new { @class = "form-control" })
                            @Html.ValidationMessage("branchId", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="col-2">
                        <div class="form-group">
                            <label class="control-label   ">Supplier</label>
                            @Html.SimpleDropDown("supplierId", "/Supplier/GetSupplierList", new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="col-1">
                        <div class="form-group">
                            <label class="control-label   mendatory" title="From date">From date</label>
                            @Html.DatePicker("dateFrom", new { @class = "form-control", data_val = "true", data_val_required = "This is required" })
                            @Html.ValidationMessage("dateFrom", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="col-1">
                        <div class="form-group">
                            <label class="control-label   mendatory">To date</label>
                            @Html.DatePicker("dateTo", new { @class = "form-control", data_val = "true", data_val_required = "This is required" })
                            @Html.ValidationMessage("dateTo", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="col-4">
                        <div class="form-group ">
                            <div class="submit-button">
                                <button type="submit" class="btn btn-bg offset-1">Show</button>
                                @Html.ActionLink("Report", "RdlcReportPurchaseSummary", "SuperShopPurchase", new { target = "_blank", id = "btnReport", @class = "btn btn-bg pull-middle" })
                                @Html.ActionLink("Detail Report", "PurchaseList", "SuperShopPurchase", new { @class = "btn btn-bg pull-middle" })
                                <input type="button" value="Clear" class="btn btn-warning offset-1 col-2" id="btnClear" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
<div class="row">
    <div class="col-12">
        <div class="panel panel-default">
            <div class="panel-body">
                <div class="dataTable_wrapper">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead class="thead-heading">
                            <tr>
                                <th>Purchase No.</th>
                                <th>Purchase Date</th>
                                <th>Supplier Name</th>
                                <th>Total Quantity</th>
                                <th>Total Amount</th>
                                <th>Memo Discount</th>
                                <th>Net Amount</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null && Model.Any())
                            {
                                foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Id)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.PurchaseDate)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.SupplierName)
                                        </td>
                                        <td class="text-right">
                                            @Html.DisplayFor(modelItem => item.TotalQuantity)
                                        </td>
                                        <td class="text-right">
                                            @Html.DisplayFor(modelItem => item.TotalAmount)
                                        </td>
                                        <td class="text-right">
                                            @Html.DisplayFor(modelItem => item.MemoWiseDiscount)
                                        </td>
                                        <td class="text-right">
                                            @Html.DisplayFor(modelItem => item.NetAmount)
                                        </td>
                                        <td>
                                            @Html.ActionLink("Report ", "ReportPurchaseMasterDetail", "SuperShopPurchase", new { companyId = item.CompanyId, branchId = item.BranchId, purchaseId = item.Id }, new { target = "_blank", @class = "btn-sm btn-bg fa fa-edit" })
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                        <tfoot>
                            @if (Model != null && Model.Any())
                            {
                                <tr class="text-right font-bold">
                                    <td colspan="3">Total:</td>
                                    <td>@Html.Raw(Model.Sum(x => Math.Round(x.TotalQuantity, 2)))</td>
                                    <td>@Html.Raw(Model.Sum(x => Math.Round(x.TotalAmount, 2)))</td>
                                    <td>@Html.Raw(Model.Sum(x => Math.Round(x.MemoWiseDiscount ?? 0, 2)))</td>
                                    <td>@Html.Raw(Model.Sum(x => Math.Round(x.NetAmount, 2)))</td>
                                    <td></td>
                                </tr>
                            }
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script type="text/javascript">
        $(function () {
            var dateFrom = getUrlVars()["dateFrom"];
            $("#dateFrom").val(dateFrom);
            var dateTo = getUrlVars()["dateTo"];
            $("#dateTo").val(dateTo);
        });

        $("#btnReport").click(function () {
            var companyId = $('#companyId').val();
            var branchId = $('#branchId').val();
            var dateFrom = $('#dateFrom').val();
            var dateTo = $('#dateTo').val();
            var supplierId = $('#supplierId').val();
            this.href = this.href.split('?')[0] + '?companyId=' + companyId
                + '&branchId=' + branchId
                + '&dateFrom=' + dateFrom
                + '&dateTo=' + dateTo
                + '&supplierId=' + supplierId;
        });

        $("#btnClear").on("click", function () {
            $("#companyId").val('').trigger("change");
            $("#branchId").val('').trigger("change");
            $("#supplierId").val('').trigger("change");
            $("#dateFrom").val('').trigger("change");
            $("#dateTo").val('').trigger("change");
        });
    </script>
}
